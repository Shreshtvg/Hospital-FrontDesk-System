fastapi==0.95.2
uvicorn==0.22.0
SQLAlchemy==1.4.46
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.5

# Database Support
alembic==1.8.1  # Database migrations

# Email Validation & Sending
email-validator==1.3.0  # Ensures valid email format
smtplib  # Built-in Python SMTP (no need to install separately)

# Data Validation & Utility
pydantic==1.10.7  # Data validation
